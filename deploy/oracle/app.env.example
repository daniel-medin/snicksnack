# Copy this file to deploy/oracle/app.env if you want local deploy-specific values.
# deploy/oracle/app.env is ignored by git and uploaded to /opt/stacks/snicksnack/.env.

ASPNETCORE_ENVIRONMENT=Production
ASPNETCORE_URLS=http://+:8080
